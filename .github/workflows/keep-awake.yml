name:  Keep Render App Awake

on:
  schedule:
    - cron: "*/10 * * * *"  
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: ⏱ Ping Render App
        run: |
          curl --silent --show-error --fail https://erpmanager.onrender.com || echo " App not reachable"
